--AFTERNOON PROJECT FOR DB 5/26/2020
--AUTHOR: Ben Doggett

--TABLE PERSON

--step 1
-- CREATE TABLE person (
-- 	person_id SERIAL,
--   name VARCHAR(40),
--   age INTEGER,
--   height INTEGER,
--   city VARCHAR(40),
--   favorite_color VARCHAR(40)
-- );
--step2
--  INSERT INTO person (
--  	name,
--   age,
--   height,
--   city, 
--   favorite_color
--  ) VALUES (
--   	'Grey',
--    	4,
--    	71,
--    	'Twin Falls',
--    	'Blue'
--    );
--step 3 
-- SELECT * FROM person ORDER BY height DESC;
--step 4
-- SELECT * FROM person ORDER BY height ASC;
--step 5 
-- SELECT * FROM person ORDER BY age DESC;
--step 6
-- SELECT * FROM person WHERE age > 20;
--step 7 
-- SELECT * FROM person WHERE age = 18;
--step 8
-- SELECT * FROM person WHERE age < 20 OR age > 30;
--step 9
-- SELECT * FROM person WHERE age <> 27;
--step 10
-- SELECT * FROM person WHERE favorite_color != 'red';
--step 11
-- SELECT * FROM person WHERE favorite_color <> 'Red' AND favorite_color <> 'Blue';
--step 12
-- SELECT * FROM person WHERE favorite_color = 'Green' OR favorite_color = 'Orange';
--step 13
-- SELECT * FROM person WHERE favorite_color IN ('Orange', 'Green', 'Blue');
--step 14
-- SELECT * FROM person WHERE favorite_color IN ('Yellow', 'Purple');

--TABLE ORDERS

--step 1
-- CREATE TABLE orders (
-- 	order_id SERIAL PRIMARY KEY,
--   person_id INTEGER,
--   product_name VARCHAR(40),
--   product_price FLOAT,
--   quantity INTEGER
-- );
--step 2
-- INSERT INTO orders(
-- 	person_id,
--   product_name,
--   product_price,
--   quantity
-- ) VALUES(
--   2,
--   'Mac n Cheese',
--   7.99,
--   1
-- );
--step 3
-- SELECT * FROM orders;
--step 4
-- SELECT sum(quantity) FROM orders;
--step 5
-- SELECT sum(product_price) FROM orders;
--step 6
-- SELECT sum(product_price) FROM orders WHERE person_id = 0;

--TABLE ARTIST

--step 1
-- INSERT INTO artist (
-- 	name
-- )VALUES (
--   'Shiny Toy Guns'
--  );
--step 2
-- SELECT name FROM artist WHERE artist_id <= 10 ORDER BY name DESC;
--step 3 
-- SELECT name FROM artist WHERE artist_id <= 5 ORDER BY name ASC;
--step 4
-- SELECT * FROM artist WHERE name LIKE 'Black%';
--step 5
-- SELECT * FROM artist WHERE name LIKE '%Black%';

--TABLE EMPLOYEE

--step 1
-- SELECT first_name, last_name FROM employee WHERE city = 'Calgary';
--step 2
-- SELECT max(birth_date) FROM employee;
--step 3
-- SELECT min(birth_date) FROM employee;
--step 4
-- SELECT first_name, last_name FROM employee WHERE reports_to = 2;
--step 5
-- SELECT count(*) FROM employee WHERE city = 'Lethbridge';

--TABLE INVOICE 

--step 1
-- SELECT COUNT(total) FROM invoice WHERE billing_country = 'USA';
--step 2
-- SELECT max(total) FROM invoice; 
--step 3
-- SELECT min(total) FROM invoice; 
--step 4
-- SELECT * FROM invoice WHERE total > 5;
--step 5 
-- SELECT count(*) FROM invoice WHERE total < 5;
--step 6
-- SELECT count(total) FROM invoice WHERE billing_state IN ('CA','TX','AZ');
--step 7 
-- SELECT avg(total) FROM invoice;
--step 8 
-- SELECT sum(total) FROM invoice;
